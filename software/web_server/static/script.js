//Simple RESTful API
import 

function startPSO() {
    let mode = document.getElementById('mode').value;

    switch(mode) {
        case 'line': 

            break;

        case 'plane': 

            break;

        case 'cube': 

            break;
    }
}

function clear() {
    
}